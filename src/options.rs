
// use std::sync::Arc;
// use systems::FileSystem;

// mod systems;

pub struct Options {
    pub verbose: bool,
    pub recurse: bool,
    pub multithread: bool,
    pub template: String,
    // pub system: Arc<systems::FileSystem>,
}